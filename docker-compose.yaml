version: "3.10"
services:
  fastline:
    build:
      context: ./fastline
      dockerfile: Dockerfile
    image: fastline:v1
    environment:
      - PORT=8020
    env_file:
      - ./fastline/.env
    ports:
      - "8001:8020"
    